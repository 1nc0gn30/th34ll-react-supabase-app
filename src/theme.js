import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'dark',
    background: {
      default: '#000',
    },
    primary: {
      main: '#0f0',
    },
    secondary: {
      main: '#0f0',
    },
    text: {
      primary: '#0f0',
    },
  },
});

export default theme;
